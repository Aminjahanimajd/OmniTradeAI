import{useEffect,useState}from'react';import{Alert,Button,Card,CardContent,MenuItem,Stack,TextField,Typography}from'@mui/material';import SaveIcon from'@mui/icons-material/Save';import{getProfile,saveProfile}from'../api';import type{UserProfile}from'../types';
export default function ProfilePage(){const[profile,setProfile]=useState<UserProfile>();const[message,setMessage]=useState('');useEffect(()=>{void getProfile().then(setProfile).catch(e=>setMessage(String(e)))},[]);if(!profile)return<Alert severity="info">Loading profile...</Alert>;const config=profile.default_configuration;return<Stack spacing={2} sx={{maxWidth:850}}><Typography variant="h4" fontWeight={800}>Profile and Defaults</Typography><Typography color="text.secondary">Saved defaults fill the New Analysis form. Each run still stores its own choices.</Typography>{message&&<Alert severity={message==='Profile saved'?'success':'error'}>{message}</Alert>}<Card><CardContent><Stack spacing={2}><TextField label="Display name" value={profile.display_name} onChange={e=>setProfile({...profile,display_name:e.target.value})}/><TextField label="Email" value={profile.email} onChange={e=>setProfile({...profile,email:e.target.value})}/><TextField label="Default stock ticker" value={profile.default_ticker} onChange={e=>setProfile({...profile,default_ticker:e.target.value.toUpperCase()})}/><TextField select label="Default data mode" value={config.data_mode} onChange={e=>setProfile({...profile,default_configuration:{...config,data_mode:e.target.value}})}><MenuItem value="recorded">Recorded</MenuItem><MenuItem value="prefer_live">Prefer live</MenuItem><MenuItem value="live">Live only</MenuItem></TextField><TextField select label="Default risk profile" value={config.risk_profile} onChange={e=>setProfile({...profile,default_configuration:{...config,risk_profile:e.target.value}})}><MenuItem value="conservative">Conservative</MenuItem><MenuItem value="balanced">Balanced</MenuItem><MenuItem value="aggressive">Aggressive</MenuItem></TextField><TextField type="number" label="Default research depth" value={config.research_depth} onChange={e=>setProfile({...profile,default_configuration:{...config,research_depth:Number(e.target.value)}})}/><TextField select label="Default report detail" value={config.report_detail} onChange={e=>setProfile({...profile,default_configuration:{...config,report_detail:e.target.value}})}><MenuItem value="summary">Summary</MenuItem><MenuItem value="standard">Standard</MenuItem><MenuItem value="detailed">Detailed</MenuItem></TextField><Button variant="contained" startIcon={<SaveIcon/>} onClick={async()=>{try{setProfile(await saveProfile(profile));setMessage('Profile saved')}catch(e){setMessage(String(e))}}}>Save profile</Button></Stack></CardContent></Card></Stack>}
